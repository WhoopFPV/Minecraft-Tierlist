# Minecraft-Tierlist
A minecraft tierlist based on elo and not lt5......
Also has support for the tiertagger mod for api v1 and v2

# How to run
`pip install pycord waitress flask requests`
`py mcelobot.py`
